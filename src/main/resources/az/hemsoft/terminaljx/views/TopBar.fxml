<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import com.jfoenix.controls.JFXButton?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<HBox alignment="CENTER_LEFT" prefHeight="70.0" styleClass="top-bar" stylesheets="@../../../../styles.css" xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1" fx:controller="az.hemsoft.terminaljx.ui.controller.TopBarController">
    <padding>
        <Insets left="10.0" right="10.0" />
    </padding>

    <!-- Area Buttons ScrollPane (First item in Flutter) -->
    <ScrollPane fitToHeight="true" hbarPolicy="NEVER" styleClass="area-scroll-pane" vbarPolicy="NEVER" HBox.hgrow="ALWAYS">
        <content>
            <HBox fx:id="areaContainer" alignment="CENTER_LEFT" spacing="10.0">
                <padding>
                    <Insets bottom="5.0" left="5.0" right="5.0" top="5.0" />
                </padding>
            </HBox>
        </content>
    </ScrollPane>

    <Region prefWidth="10.0" />

    <!-- New Order Button -->
    <JFXButton fx:id="newOrderButton" mnemonicParsing="false" onAction="#handleNewOrder" styleClass="new-order-button" text="+ Yeni Sifariş" />

    <Region prefWidth="10.0" />

    <!-- Checks (Cekler) Button -->
    <JFXButton fx:id="checksButton" mnemonicParsing="false" onAction="#handleChecks" styleClass="checks-button" text="Çeklər">
         <graphic>
            <Region styleClass="icon-checks" />
         </graphic>
    </JFXButton>

    <Region prefWidth="10.0" />

    <!-- User/Back Button (Exit System) -->
    <JFXButton fx:id="userBackButton" contentDisplay="LEFT" graphicTextGap="10.0" onAction="#handleBack" styleClass="user-back-button" text="Admin User">
        <graphic>
            <Region styleClass="icon-back" />
        </graphic>
    </JFXButton>

    <Region prefWidth="15.0" />

    <!-- Status Icons -->
    <HBox alignment="CENTER" spacing="12.0" styleClass="status-container">
        <padding>
            <Insets bottom="5.0" left="15.0" right="15.0" top="5.0" />
        </padding>
        <HBox alignment="CENTER" styleClass="status-item-container">
            <Label styleClass="status-icon-connected" text="📶" />
        </HBox>
        <HBox alignment="CENTER" styleClass="status-item-container">
            <Label styleClass="status-icon-notification" text="🔔" />
        </HBox>
        <JFXButton onAction="#handleMenu" styleClass="menu-button-icon" text="">
             <graphic>
                <HBox alignment="CENTER" styleClass="status-item-container">
                    <Label styleClass="status-icon-menu" text="☰" />
                </HBox>
             </graphic>
        </JFXButton>
    </HBox>
</HBox>
